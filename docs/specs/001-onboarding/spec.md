# [001] 온보딩 — 스펙

**상태** Draft
**브랜치** `001-onboarding`
**작성** 2026-08-19

> 이 문서에 프레임워크 이름이 등장하면 잘못 쓴 것이다.
> *무엇을·왜*만 적는다. *어떻게*는 `plan.md`.

---

## 1. 문제

앱은 모든 사용자에게 같은 등급을 보여준다. 그런데 같은 체감온도에서도
땀이 잘 나는 사람과 아닌 사람의 경험은 다르다. 이동수단과 야외 노출 시간도
사람마다 달라서, 같은 경로라도 부담이 같지 않다.

`SweatStageEngine`은 이미 민감도를 받을 준비가 되어 있지만
(`Sensitivity`, 000에서 구현), **그 값을 물어보고 기억하는 곳이 없다.**
지금은 항상 `.normal`로 계산된다.

## 2. 이 기능이 끝나면

처음 앱을 연 사용자가 세 가지를 답하고 홈으로 들어간다 — 땀 민감도, 이동 패턴,
알림 수신 여부. 답한 값은 기기에 남아 다음 실행부터 등급 계산에 쓰인다.
나중에 마이페이지에서 다시 고칠 수 있다.

## 3. 사용자 시나리오

### 최초 실행
```
Given  앱을 처음 실행한다
When   민감도 → 이동 패턴 → 알림 순서로 답한다
Then   홈이 뜨고, 고른 민감도가 반영된 등급이 보인다
```

### 재실행
```
Given  온보딩을 마친 적이 있다
When   앱을 실행한다
Then   온보딩 없이 홈이 바로 뜬다
```

### 값 수정
```
Given  마이페이지에서 "내 땀 민감도 다시 설정"을 누른다
When   값을 바꾸고 저장한다
Then   마이페이지로 돌아가고, 바뀐 값이 프로필 요약에 보인다
```

### 예외
| 상황 | 결과 |
|---|---|
| 알림 권한 거부 | 온보딩은 정상 완료된다. 알림만 오지 않는다 |
| "나중에 할게요" | 권한을 요청하지 않고 완료한다 |
| 이미 권한이 있는 상태로 재진입 | 다시 묻지 않는다 |
| 온보딩 도중 앱 종료 | 다음 실행에 온보딩을 처음부터 다시 한다 |

마지막 항목은 의도된 단순화다. 3단계짜리 흐름에 중간 저장을 넣을 이유가 없다.

## 4. 요구사항

| ID | 요구사항 | 검증 방법 |
|---|---|---|
| R1 | 온보딩을 마치기 전에는 앱을 다시 켜도 온보딩이 뜬다 | 완료 전 종료 후 재실행 |
| R2 | 온보딩을 마치면 다음 실행부터 홈으로 바로 간다 | 완료 후 재실행 |
| R3 | 고른 민감도·이동수단·이동시간이 앱을 껐다 켜도 유지된다 | 재실행 후 마이페이지에서 확인 |
| R4 | 고른 민감도가 등급 계산에 실제로 반영된다 | 같은 체감온도에서 민감도만 바꿔 등급이 달라지는지 |
| R5 | 각 단계는 기본값이 이미 선택된 상태로 시작한다 (보통 · 지하철 · 20~40분) | 화면 진입 시 |
| R6 | "알림 허용하고 시작"은 시스템 권한을 요청하고, 결과와 무관하게 온보딩을 끝낸다 | 허용·거부 양쪽 |
| R7 | "나중에 할게요"는 권한을 요청하지 않고 온보딩을 끝낸다 | 시스템 대화상자가 뜨지 않음 |
| R8 | 마이페이지에서 재진입하면 뒤로가기가 생기고 CTA가 `저장`으로 바뀐다 | 두 경로 비교 |
| R9 | 재진입 시 저장된 값이 선택된 상태로 보인다 | 값을 바꾼 뒤 재진입 |
| R10 | 재진입에서 저장하면 마이페이지로 돌아간다 (홈이 아니라) | |
| R11 | 최초 온보딩에는 탭 바가 없다. 재진입 시에는 있다 | 두 경로 비교 |
| R12 | 선택지가 VoiceOver에서 선택 상태로 읽힌다 | VoiceOver 켜고 순회 |
| R13 | 접근성 글자 크기에서 문구가 잘리지 않는다 | `AX5`에서 세 화면 |
| R14 | 화면 문구가 §6과 정확히 일치한다 | 대조 |

## 5. 화면

| Figma | 화면 | 비고 |
|---|---|---|
| `01 온보딩 · 땀 민감도` | 민감도 3지선다 | 재진입 시 상단에 뒤로가기 |
| `02 온보딩 · 이동 패턴` | 이동수단 4 + 이동시간 3 | 재진입 시 상단에 뒤로가기 |
| `03 온보딩 · 알림 권한` | 알림 미리보기 + 허용/나중에 | 재진입 없음 |

진입점은 `10 마이페이지`의 `내 땀 관리` 메뉴 두 항목이다.

**높이는 Figma를 목표로 삼지 않는다** — `docs/design-source.md` 참조.
간격과 순서만 맞춘다.

## 6. 문구

### 공통
| 키 | 문구 | 조건 |
|---|---|---|
| `cta.next` | 다음 | 최초 온보딩 |
| `cta.save` | 저장 | 마이페이지에서 재진입 |
| `back.profile` | ← 마이페이지 | 재진입 시에만 |

### 01 민감도
| 키 | 문구 |
|---|---|
| `step` | STEP 1 / 3 |
| `heading` | 땀은 얼마나\n많이 흘리는 편인가요? |
| `sub` | 같은 날씨에서도 사람마다 느끼는 정도가 달라요. 등급과 추천을 조정하는 데 쓰입니다. |
| `option.low` | 적은 편 — 더워도 땀은 잘 안 나는 편이에요 |
| `option.normal` | 보통 — 남들과 비슷한 정도예요 |
| `option.high` | 많은 편 — 조금만 더워도 땀이 많이 나요 |

### 02 이동 패턴
| 키 | 문구 |
|---|---|
| `step` | STEP 2 / 3 |
| `heading` | 평소 이동은\n어떻게 하시나요? |
| `sub` | 이동수단과 시간은 실외 노출 계산과 휴식 추천에 쓰입니다. |
| `label.transport` | 주 이동수단 |
| `transport.*` | 도보 · 지하철 · 버스 · 자전거 |
| `label.duration` | 하루 야외 이동 시간 |
| `duration.*` | 20분 이하 · 20~40분 · 40분 이상 |

### 03 알림
| 키 | 문구 |
|---|---|
| `step` | STEP 3 / 3 |
| `heading` | 외출 전에\n알려드릴까요? |
| `sub` | 땀 부담이 높은 날 아침, 그리고 이동 중 더위가 심해질 때만 보냅니다. |
| `preview.label` | 미리보기 |
| `preview.title` | 오늘은 땀이 많이 날 수 있어요 |
| `preview.body` | 체감온도가 높아요. 가능하면 그늘이 많은 길로 이동해보세요. |
| `cta.allow` | 알림 허용하고 시작 |
| `cta.later` | 나중에 할게요 |

### 헌법 III 점검
전부 `~일 수 있어요`·`~해보세요` 어투다. 분비량 수치·배수·등식 표현 없음.
`preview.*`는 알림 문구이므로 화면 카피(`SweatCopy`)와 별개로 관리한다.

> **Figma와 개발가이드가 한 글자 다르다** — Figma는 `오늘은 땀 많이 날 수 있어요`,
> 개발가이드 §7은 `오늘은 땀이 많이 날 수 있어요`. **개발가이드를 따른다.**
> 문구의 출처는 개발가이드이고 Figma는 그것을 옮긴 것이므로, 어긋나면 원문이 맞다.
> Figma도 함께 고친다.

## 7. 범위 밖

| | 어디서 |
|---|---|
| 실제 알림 스케줄링 (아침 브리핑) | 별도 스펙 |
| 위치 권한 요청 | 002 |
| 이동수단·이동시간이 경로 계산에 반영되는 것 | 003 |
| 마이페이지 화면 자체 | 005 |
| 프로필 편집(이름·아바타) | 범위 밖 |

**005보다 001이 먼저인데 진입점이 마이페이지라는 문제** — 재진입 흐름(R8~R11)은
005에서 마이페이지가 생겨야 실제로 눌러볼 수 있다. 001에서는 재진입 **모드**를
구현하고, 임시 진입점으로 검증한다. 005에서 마이페이지가 연결한다.

## 8. 미해결 질문

- [ ] **저장 방식** — 000에서 SwiftData 스키마를 005로 미뤘는데, 온보딩은 지금
      저장해야 한다. 가벼운 키-값 저장으로 시작해 005에서 옮길지, 여기서
      영속화 계층을 시작할지. 옮기면 마이그레이션이 생긴다.
- [ ] **이동수단·이동시간을 도메인 타입으로 만들지** — 민감도는 `Sensitivity`로
      도메인에 있다. 나머지 둘은 001에서 소비처가 없고 003에서 쓰인다.
      지금 타입을 만들면 쓰이지 않는 도메인이 생기고, 문자열로 두면 003에서
      바꿔야 한다.
- [ ] **권한 거부 후 재요청 경로** — 마이페이지 설정에 `외출 전 알림 · 켜짐`이
      있다. 거부 상태에서 이걸 누르면 시스템 설정으로 보낼지, 안내만 할지.
      005 범위일 수도 있다.
- [ ] **토큰 밖 여백 · Chip 터치 타깃** — 000 §8에서 넘어온 미결.
      `plan.md`를 쓰기 전에 정해야 한다. 스펙은 막지 않는다.

## 9. 완료 조건

- [ ] R1~R14 전부 검증됨
- [ ] 접근성: VoiceOver 선택 상태, 접근성 글자 크기에서 잘림 없음
- [ ] 문구가 §6과 일치하고 헌법 III 위반 없음
- [ ] `Scripts/lint-tokens.sh` 통과 (대상 경로에 Features 추가)
- [ ] 빌드 경고 0, 테스트 통과
- [ ] `docs/specs/README.md`의 001 상태 갱신

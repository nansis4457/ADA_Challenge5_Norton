# [002] 홈 · 등급 상세 — 태스크

**스펙** `docs/specs/002-home/spec.md`
**브랜치** `002-home`

> 태스크 하나 = 커밋 하나. `[P]`는 병렬 가능.
> git 명령은 사람이 실행한다 (`docs/git-workflow.md` §0).

---

## 🚫 착수 조건

- [x] T000 **기상청 체감온도 산식 원문 대조** — 2026-08-20 완료
      계수는 문서에 적혀 있던 값과 일치했다. 결과를 `architecture.md` §5.4에 반영.
      **디자인의 34.2℃는 틀렸다 — 실제 33.5℃.** 단계는 3단계로 동일하다.
      기상청 페이지를 직접 열지 못해 1차 출처 눈확인이 남아 있다 (§5.4 경고).
- [ ] T001 WeatherKit capability 활성화 (Apple Developer 계정)
- [ ] T002 Apple Weather 출처 표기 요건 확인 — **추측해서 넣지 않는다**
- [ ] T002b Figma 정정 — 체감 34.2℃ → 33.5℃, 헤더 출처 문구 변경
- [ ] T003 브랜치 `002-home` 생성, 스펙 Approved

## 도메인 — 네트워크 없이 전부 검증된다

- [x] T010 `ApparentTemperature` — 산식 (순수 함수)
      `feat(domain): 체감온도 산식 추가`
- [x] T011 산식 테스트 — **공표 사례 대조**. 스펙의 전제를 고정한다
      `test(domain): 체감온도 산식을 공표 사례로 검증`
- [ ] T012 [P] `Observation` — 관측값 + 출처
      `feat(domain): 관측값 모델 추가`
- [ ] T013 [P] `ForecastLevel` — 6단계 → 표시용 4구간
      `feat(domain): 예보 표시 단계 매핑 추가`
- [ ] T014 규칙 회귀 테스트 — 습도만 바꿔도 단계 불변 (R8)
      `test(domain): 습도·풍속이 단계를 바꾸지 않음을 검증`

## 날씨 데이터 — 소스는 WeatherKit 하나

- [ ] T020 `WeatherData` 패키지 생성 + 링크
      `chore(weather): WeatherData 패키지 추가`
- [ ] T021 `WeatherKitSource` — 기온·습도·풍속을 `Observation`으로
      습도 단위(0~1 ↔ %) 변환에 주의한다
      `feat(weather): WeatherKit 소스 어댑터 추가`
- [ ] T022 캐시 — 메모리 10분 · 디스크 1시간(stale 허용)
      **폴백 소스가 없으므로 캐시가 유일한 방어선이다**
      `feat(weather): 관측값 캐시 추가`
- [ ] T023 `WeatherRepository` — 조회·캐시·재시도, 값에 출처 부착
      `feat(weather): 날씨 저장소 추가`
- [ ] T024 저장소 테스트 — 타임아웃·오류·오프라인·stale 반환 **(R10, R11)**
      `test(weather): 실패 경로와 캐시 검증`

## 위치

- [ ] T030 위치 권한 요청 + 현재 위치 획득
      `feat(home): 위치 권한과 현재 위치 추가`
- [ ] T031 권한 거부 시 지역 선택 **(R9)**
      `feat(home): 위치 없이 지역을 고르는 경로 추가`

## 홈 화면

- [ ] T040 `HomeStore` — 관측 → 단계 → 문구
      `feat(home): 홈 상태 저장소 추가`
- [ ] T041 위치 헤더 + 관측값 3칸 **(R1, R3)**
      `feat(home): 위치 헤더와 관측값 표시 추가`
- [ ] T042 마스코트 6단계 — `Shape`/`Path`로 구현 **(R4)**
      단계 전환이 색·표정·땀방울로 보간되어야 한다
      `feat(home): 단계별 마스코트 추가`
- [ ] T043 멘트 + `더 알아보기` **(R2)**
      `feat(home): 단계 멘트와 상세 진입 추가`
- [ ] T044 시간별 예보 **(R5)**
      `feat(home): 시간별 예보 섹션 추가`
- [ ] T045 주간 예보 — 범위 막대 **(R6)**
      `feat(home): 주간 예보 섹션 추가`
- [ ] T046 로딩·실패 상태 **(R11)** — `n분 전 관측` 배지
      `feat(home): 오프라인과 실패 상태 표시 추가`
- [ ] T047 접근성 크기에서 예보를 세로 목록으로 **(R13)**
      `feat(home): 접근성 크기 대체 배치 추가`

**프로토타입의 체감온도 슬라이더는 만들지 않는다.**

## 등급 상세

- [ ] T050 기여도 막대 3종 **(R7)**
      막대는 **설명**이지 계산이 아니다
      `feat(home): 등급 상세 기여도 표시 추가`
- [ ] T051 각주 + 뒤로가기
      `feat(home): 등급 상세 각주 추가`

## 마무리

- [ ] T090 콜드 스타트 계측 **(R12)** — 1.5초
- [ ] T091 접근성 확인 — VoiceOver 마스코트 라벨, `AX3` 예보 **(R13, R14)**
- [ ] T092 수동 검증 — 6단계, 권한 거부, 오프라인, 폴백
- [ ] T093 린트·테스트·Debug/Release 빌드
- [ ] T094 규칙 점검 표 최종 확인
- [ ] T095 로드맵 갱신, PR

---

## 의존 관계

```
T000 (산식 검증) ── 이게 안 되면 전부 멈춘다
   │
   ├─→ T010 → T011
   │      └─→ T012 · T013 · T014
   │
   └─→ T020 → T021 → T022 → T023 → T024
                                       │
                       T030 → T031 ────┤
                                       ↓
                             T040 → T041~T047
                                       └─→ T050 · T051
```

**T000(산식)은 끝났다.** 이제 T001(capability)만 없으면 날씨 쪽이 막힌다.

도메인(T010~T014)은 네트워크·권한·계정 설정 없이 전부 검증된다. 여기부터 시작할 수 있다.

---

## 진행

| 날짜 | 완료 | 비고 |
|---|---|---|
| 2026-08-20 | T010·T011 | 산식 구현 + 기준 케이스 6개 고정. **처음 쓴 기대값 3개가 지어낸 값이라 실패 → 계산한 값으로 교체** |
| 2026-08-20 | T000 | 산식 검증. 계수 일치, 디자인 수치 0.73℃ 오차, 단계는 불변. **풍속은 산식에 안 들어감** 발견 |

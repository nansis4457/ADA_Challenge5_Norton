# [002] 홈 · 등급 상세 — 태스크

**스펙** `docs/specs/002-home/spec.md`
**브랜치** `002-home`

> 태스크 하나 = 커밋 하나. `[P]`는 병렬 가능.
> git 명령은 사람이 실행한다 (`docs/git-workflow.md` §0).

---

## 🚫 착수 조건

- [ ] T000 **기상청 체감온도 산식 원문 대조 완료**
      결과를 `docs/architecture.md` §5.4에 반영한다.
      **이게 끝나기 전에는 아래로 넘어가지 않는다.** 화면의 모든 숫자가 여기서 나온다.
- [ ] T001 공공데이터포털 인증키 발급, `Secrets.xcconfig` 설정
- [ ] T002 대체 날씨 소스 사용 설정 + 출처 표기 조건 확인
- [ ] T003 브랜치 `002-home` 생성, 스펙 Approved

## 도메인 — 네트워크 없이 전부 검증된다

- [ ] T010 `ApparentTemperature` — 산식 (순수 함수)
      `feat(domain): 체감온도 산식 추가`
- [ ] T011 산식 테스트 — **공표 사례 대조**. 스펙의 전제를 고정한다
      `test(domain): 체감온도 산식을 공표 사례로 검증`
- [ ] T012 [P] `Observation` — 관측값 + 출처
      `feat(domain): 관측값 모델 추가`
- [ ] T013 [P] `ForecastLevel` — 6단계 → 표시용 4구간
      `feat(domain): 예보 표시 단계 매핑 추가`
- [ ] T014 규칙 회귀 테스트 — 습도만 바꿔도 단계 불변 (R8)
      `test(domain): 습도·풍속이 단계를 바꾸지 않음을 검증`

## 날씨 데이터

- [ ] T020 `WeatherData` 패키지 생성 + 링크
      `chore(weather): WeatherData 패키지 추가`
- [ ] T021 격자 변환 + 테스트 (알려진 좌표쌍 고정)
      `feat(weather): 위경도-격자 변환 추가`
- [ ] T022 발표 시각 계산 + 테스트 (매시 40분 경계)
      `feat(weather): 관측 발표 시각 계산 추가`
- [ ] T023 카테고리 파싱 + 테스트 (정상·미지 코드·결측)
      `feat(weather): 관측 응답 파싱 추가`
- [ ] T024 1순위 소스 클라이언트
      `feat(weather): 기상청 클라이언트 추가`
- [ ] T025 대체 소스 어댑터 — 기온·습도로 **우리 산식을 다시 적용**한다
      `feat(weather): 대체 날씨 소스 어댑터 추가`
- [ ] T026 캐시 — 메모리 10분 · 디스크 1시간(stale 허용)
      `feat(weather): 관측값 캐시 추가`
- [ ] T027 `WeatherRepository` — 폴백 순서와 출처 표기
      `feat(weather): 날씨 저장소와 폴백 경로 추가`
- [ ] T028 저장소 테스트 — 타임아웃·빈 응답·오류·오프라인 **(R10, R11)**
      `test(weather): 폴백과 캐시 경로 검증`

## 위치

- [ ] T030 위치 권한 요청 + 현재 위치 획득
      `feat(home): 위치 권한과 현재 위치 추가`
- [ ] T031 권한 거부 시 지역 선택 **(R9)**
      `feat(home): 위치 없이 지역을 고르는 경로 추가`

## 홈 화면

- [ ] T040 `HomeStore` — 관측 → 단계 → 문구
      `feat(home): 홈 상태 저장소 추가`
- [ ] T041 위치 헤더 + 관측값 3칸 **(R1, R3)**
      `feat(home): 위치 헤더와 관측값 표시 추가`
- [ ] T042 마스코트 6단계 — `Shape`/`Path`로 구현 **(R4)**
      단계 전환이 색·표정·땀방울로 보간되어야 한다
      `feat(home): 단계별 마스코트 추가`
- [ ] T043 멘트 + `더 알아보기` **(R2)**
      `feat(home): 단계 멘트와 상세 진입 추가`
- [ ] T044 시간별 예보 **(R5)**
      `feat(home): 시간별 예보 섹션 추가`
- [ ] T045 주간 예보 — 범위 막대 **(R6)**
      `feat(home): 주간 예보 섹션 추가`
- [ ] T046 로딩·실패 상태 **(R11)** — `n분 전 관측` 배지
      `feat(home): 오프라인과 실패 상태 표시 추가`
- [ ] T047 접근성 크기에서 예보를 세로 목록으로 **(R13)**
      `feat(home): 접근성 크기 대체 배치 추가`

**프로토타입의 체감온도 슬라이더는 만들지 않는다.**

## 등급 상세

- [ ] T050 기여도 막대 3종 **(R7)**
      막대는 **설명**이지 계산이 아니다
      `feat(home): 등급 상세 기여도 표시 추가`
- [ ] T051 각주 + 뒤로가기
      `feat(home): 등급 상세 각주 추가`

## 마무리

- [ ] T090 콜드 스타트 계측 **(R12)** — 1.5초
- [ ] T091 접근성 확인 — VoiceOver 마스코트 라벨, `AX3` 예보 **(R13, R14)**
- [ ] T092 수동 검증 — 6단계, 권한 거부, 오프라인, 폴백
- [ ] T093 린트·테스트·Debug/Release 빌드
- [ ] T094 규칙 점검 표 최종 확인
- [ ] T095 로드맵 갱신, PR

---

## 의존 관계

```
T000 (산식 검증) ── 이게 안 되면 전부 멈춘다
   │
   ├─→ T010 → T011
   │      └─→ T012 · T013 · T014
   │
   └─→ T020 → T021 · T022 · T023 → T024 → T025 → T026 → T027 → T028
                                                              │
                                              T030 → T031 ────┤
                                                              ↓
                                                    T040 → T041~T047
                                                              └─→ T050 · T051
```

**T000이 유일한 진짜 블로커다.** 나머지는 순서만 지키면 막히지 않는다.

도메인(T010~T014)은 네트워크·권한 없이 전부 검증된다. 인증키 발급이 늦어져도
이쪽은 먼저 진행할 수 있다 — 단 T000은 끝나 있어야 한다.

---

## 진행

| 날짜 | 완료 | 비고 |
|---|---|---|
| | | |
